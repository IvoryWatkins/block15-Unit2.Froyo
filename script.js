// 3. Can you prompt the user for froyo flavors and store their result?
const userInput = prompt("Enter Some Froyo Flavors!");
console.log(userInput);

//4.
const flavors = {
  Vanilla: 3,
  Strawberry: 1,
  Coffee: 2,
};

console.table(flavors);